rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development-friendly rules: allow read/write to all documents.
    // WARNING: This makes your Firestore database publicly readable and writable.
    // Replace with stricter rules before production use.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}